# Project 1 : for the 'Law of Large Number'
N <- 1000
counter <- 0
for( i in rnorm(N)){
  # here we use the normal distribution to get the result near to 50:50 distribution.
  if( i < 1 & i > -1))
    counter = counter+1
  
}
result = counter/N
print(result)
   
